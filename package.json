{
  "name": "simplecalendar",
  "version": "0.0.1",
  "description": "Simple Calendar with Todo",
  "main": "index.js",
  "scripts": {
    "devServer": "npx browser-sync start --server --tunnel --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "browser-sync": "^2.26.3"
  }
}
